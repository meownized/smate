.person
  .profile_info
    .ava_name
      - if @user.avatar.present?
        = image_tag(@user.avatar)
      - else
        .avatar.default_avatar

      .user_info
        h3
          =fullname(@user)
        .p2
         = sex_age(@user)

    .badges
      .m_badge.primary_badge Курит
      .m_badge.primary_badge Есть домашнее животное

    .p2
      | Работаю дизайнером в своей веб-студии, дома бываю нечасто, в основном чтобы переночевать. Гостей парней и прочую живность не вожу. Живу с лысым котом, к лотку приучен, из комнаты не выходиит. Вообще я могу много чего о себе написать, например, ка кпочти научилась укладываться в дедлайны. Это особое умение, и я готова поделитсья им с миром.

    div
      .spacing-xl-h
    - if @user.images.present?
      h5
        | Фото

        .profile_photos
          - @user.images.each do |image|
            = image_tag(image, class: 'profile_photo')

    h5
      | Cоциальные сети
    .social_icons
      .vk
      .facebook
      .instagram

  - if @flat.present?
    .flat_card
      h5
        = @flat.name

      = image_tag(@flat.flat_attachments.first.image, class: 'main_photo')

      .small_photos
        - @flat.flat_attachments.drop(1).each do |flat_attachment|
          = image_tag(flat_attachment.image, class: 'photo')

      .p2
        | #{flat_price(@flat)} ₽ в месяц
      .p4
        = @flat.description
      .p4
        = @flat.subway
